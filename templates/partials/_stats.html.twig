<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
    <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition-shadow">
        <h3 class="text-lg font-semibold text-gray-700 mb-2">Clients</h3>
        <p class="text-4xl font-bold text-blue-600">{{ clientCount }}</p>
        <a href="{{ path('app_client_index') }}" class="text-blue-500 hover:underline mt-2 inline-block">Voir les clients</a>
    </div>

    <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition-shadow">
        <h3 class="text-lg font-semibold text-gray-700 mb-2">Projets</h3>
        <p class="text-4xl font-bold text-green-600">{{ projectCount }}</p>
        <a href="{{ path('app_project_index') }}" class="text-green-500 hover:underline mt-2 inline-block">Voir les projets</a>
    </div>

    <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition-shadow">
        <h3 class="text-lg font-semibold text-gray-700 mb-2">Devis</h3>
        <p class="text-4xl font-bold text-yellow-600">{{ estimateCount }}</p>
        <a href="{{ path('app_estimate_index') }}" class="text-yellow-500 hover:underline mt-2 inline-block">Voir les devis</a>
    </div>

    <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition-shadow">
        <h3 class="text-lg font-semibold text-gray-700 mb-2">Factures</h3>
        <p class="text-4xl font-bold text-red-600">{{ invoiceCount }}</p>
        <a href="{{ path('app_invoice_new') }}" class="text-red-500 hover:underline mt-2 inline-block">Créer une facture</a>
        <a href="{{ path('app_sales_document_index') }}" class="text-red-500 hover:underline mt-1 inline-block">Voir toutes les factures</a>
    </div>
</div>

