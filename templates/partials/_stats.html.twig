<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
    {% set cardConfig = [
        {title: 'Clients', value: clientCount, color: 'blue', link: path('app_client_index')},
        {title: 'Projets', value: projectCount, color: 'green', link: path('app_project_index')},
        {title: 'Devis', value: estimateCount, color: 'yellow', link: path('app_estimate_index')},
        {title: 'Factures', value: invoiceCount, color: 'red', link1: path('app_sales_document_index'), link2: path('app_invoice_new')}
    ] %}

    {% for card in cardConfig %}
        <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition-shadow">
            <h3 class="text-lg font-semibold text-gray-700 mb-2">{{ card.title }}</h3>
            <p class="text-4xl font-bold text-{{ card.color }}-600">{{ card.value }}</p>
            {% if card.link is defined %}
                <a href="{{ card.link }}" class="text-{{ card.color }}-500 hover:underline mt-2 inline-block">Voir {{ card.title|lower }}</a>
            {% elseif card.link1 and card.link2 %}
                <a href="{{ card.link2 }}" class="text-{{ card.color }}-500 hover:underline mt-2 inline-block">Créer {{ card.title|lower }}</a>
                <a href="{{ card.link1 }}" class="text-{{ card.color }}-500 hover:underline mt-1 inline-block">Voir toutes les {{ card.title|lower }}</a>
            {% endif %}
        </div>
    {% endfor %}
</div>
