<form method="post" action="{{ path('app_sales_document_item_delete', {'id': sales_document_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sales_document_item.id) }}">
    <button class="px-4 py-2 bg-red-500 text-white rounded transition hover:bg-red-600">Delete</button>
</form>
