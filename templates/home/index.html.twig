{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - SoloDesk{% endblock %}

{% block body %}

    <h2 class="text-3xl font-extrabold mb-8">Bienvenue sur SoloDesk 👋</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">

        <!-- Clients -->
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition-shadow">
            <h2 class="text-lg font-semibold text-gray-700 mb-4">Nouveaux clients par année</h2>
            {{ render_chart(clientDataChart) }}
        </div>

        <!-- Devis -->
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition-shadow">
            <h2 class="text-lg font-semibold text-gray-700 mb-4">Suivi des devis</h2>
            {{ render_chart(estimateChart) }}
            <p class="text-sm text-gray-500 mt-2">Taux de conversion : Accepté / Envoyé / Refusé</p>
        </div>

        <!-- Factures -->
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition-shadow">
            <h2 class="text-lg font-semibold text-gray-700 mb-4">Factures par année</h2>
            {{ render_chart(invoiceChart) }}
            <p class="text-sm text-gray-500 mt-2">Nombre total de factures émises par année</p>
        </div>

        <!-- CA par client -->
        <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition-shadow">
            <h2 class="text-lg font-semibold text-gray-700 mb-4">Répartition du CA par client</h2>
            {{ render_chart(paymentsChart) }}
            <div class="mt-4">
                <a href="{{ path('stats_ca_trimestre') }}"
                   class="inline-block px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">
                    Voir le CA par trimestre
                </a>
            </div>
        </div>

    </div>

    <!-- Stat cards -->
    {% include 'partials/_stats.html.twig' with {
        projectCount: totalProjects,
        clientCount: totalClients,
        estimateCount: totalEstimates,
        invoiceCount: totalInvoices
    } %}

{% endblock %}
